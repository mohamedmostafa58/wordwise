{
  "extends": "react-app",
  "overrides": [
    {
      "files": ["src/**/*.jsx"], // Adjust the pattern to match your file structure
      "rules": {
        "react/prop-types": "off" // Disable the prop-types rule for these files
      }
    }
  ]
}
